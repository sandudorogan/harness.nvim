{
  "name": "nvim-harness",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*"],
  "scripts": {
    "test": "bun test bootstrap.test.ts && bun run --filter harnessd test",
    "test:daemon": "bun run --filter harnessd test",
    "test:nvim": "nvim --headless -u tests/minimal_init.lua +qall",
    "typecheck": "tsc --noEmit",
    "lint": "biome check && stylua --check lua/ plugin/ tests/",
    "format": "biome check --write && stylua lua/ plugin/ tests/",
    "check": "bun run typecheck && bun run lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2"
  }
}
